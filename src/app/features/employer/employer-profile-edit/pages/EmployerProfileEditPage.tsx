import { useTranslation } from 'react-i18next';

const EmployerProfileEditPage = () => {
  const { t } = useTranslation();

  return <div>EmployerProfileEditPage: </div>;
};

export default EmployerProfileEditPage;
